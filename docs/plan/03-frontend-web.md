# 03. 웹 프론트엔드 (Next.js 15)

---

## 1. App Router 전체 라우트 트리

```
apps/web/src/app/
├── layout.tsx                      # Root Layout (providers, fonts, metadata)
├── globals.css                     # Tailwind 4 import + 시맨틱 색상 테마 변수 (light/dark)
├── providers.tsx                   # Client Providers 래퍼
├── not-found.tsx                   # 404 페이지
├── error.tsx                       # 전역 에러 바운더리
│
├── (auth)/                         # Auth 그룹 (사이드바 없는 레이아웃)
│   ├── layout.tsx                  # Auth Layout (중앙 정렬, 브랜딩)
│   ├── login/page.tsx              # 로그인
│   ├── register/page.tsx           # 회원가입
│   ├── forgot-password/page.tsx    # 비밀번호 재설정 요청
│   ├── reset-password/page.tsx     # 비밀번호 재설정 실행
│   ├── verify-email/page.tsx       # 이메일 인증
│   └── onboarding/page.tsx         # 온보딩 (최초 가입 후)
│
├── (main)/                         # Main 그룹 (사이드바 + 헤더 레이아웃)
│   ├── layout.tsx                  # Main Layout (Sidebar + Header + Content)
│   ├── page.tsx                    # 홈 (명함 리스트) - redirect or default
│   ├── cards/
│   │   ├── page.tsx                # 명함 목록 (카드/리스트/테이블 뷰)
│   │   ├── new/page.tsx            # 수동 명함 입력
│   │   └── [id]/
│   │       ├── page.tsx            # 명함 상세 (Split View 우측 패널)
│   │       └── edit/page.tsx       # 명함 편집
│   ├── scan/
│   │   ├── page.tsx                # 스캔 화면 (파일 업로드 / WebRTC)
│   │   └── confirm/page.tsx        # OCR 결과 확인/편집
│   ├── folders/
│   │   ├── page.tsx                # 폴더 관리 (트리뷰)
│   │   └── [id]/page.tsx           # 폴더별 명함 목록
│   ├── tags/page.tsx               # 태그 관리
│   └── settings/
│       ├── page.tsx                # 설정 메인
│       ├── profile/page.tsx        # 프로필 편집
│       ├── export/page.tsx         # 내보내기
│       └── help/page.tsx           # 도움말
│
└── api/v1/                         # API Routes (02-backend-api.md 참조)
    └── ...
```

---

## 2. 레이아웃 구조

### 2.1 Root Layout (`app/layout.tsx`)

```typescript
// 공통 요소:
// - <html> lang, className (Tailwind, 폰트)
// - <body> className
// - Providers 래퍼 (QueryClient, Theme, Intl, Toaster)
// - Metadata (SEO)

export const metadata: Metadata = {
  title: { default: "CardKeeper", template: "%s | CardKeeper" },
  description: "비즈니스 명함 관리 서비스",
};
```

### 2.2 Auth Layout (`(auth)/layout.tsx`)

```
┌─────────────────────────────────────────┐
│           CardKeeper Logo               │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │                                   │  │
│  │         Auth Form Card            │  │
│  │    (로그인/회원가입/비밀번호)       │  │
│  │                                   │  │
│  └───────────────────────────────────┘  │
│                                         │
│       © 2026 CardKeeper                 │
└─────────────────────────────────────────┘
```

- 중앙 정렬 레이아웃
- 최대 너비 480px 카드
- 브랜딩 로고 + 하단 링크
- 소셜 로그인 버튼 그룹

### 2.3 Main Layout (`(main)/layout.tsx`)

**Desktop (≥1024px):**
```
┌──────────┬─────────────────────────────────────────────┐
│          │  Header (검색바, 사용자 메뉴)                 │
│          ├─────────────────────────────────────────────┤
│ Sidebar  │                                             │
│          │  Content Area                               │
│ - 홈     │                                             │
│ - 스캔   │  ┌──────────────────┬──────────────────────┐│
│ - 폴더   │  │  List Panel      │  Detail Panel        ││
│ - 태그   │  │  (명함 목록)      │  (Split View)        ││
│ - 설정   │  │                  │                      ││
│          │  └──────────────────┴──────────────────────┘│
└──────────┴─────────────────────────────────────────────┘
```

**Tablet (768-1023px):**
- 사이드바 축소 (아이콘만)
- Split View 비활성화 (상세는 전체 화면)

**Mobile (< 768px):**
- 사이드바 → 햄버거 메뉴 (Sheet)
- 단일 컬럼 레이아웃

---

## 3. 컴포넌트 구조

```
apps/web/src/components/
├── ui/                             # shadcn/ui 기반 원자 컴포넌트
│   ├── button.tsx
│   ├── input.tsx
│   ├── dialog.tsx
│   ├── sheet.tsx
│   ├── dropdown-menu.tsx
│   ├── command.tsx                 # Command palette (검색)
│   ├── badge.tsx
│   ├── avatar.tsx
│   ├── skeleton.tsx
│   ├── toast.tsx (sonner)
│   ├── tooltip.tsx
│   ├── select.tsx
│   ├── checkbox.tsx
│   ├── switch.tsx
│   ├── separator.tsx
│   ├── card.tsx
│   ├── form.tsx                    # React Hook Form 연동
│   ├── label.tsx
│   ├── textarea.tsx
│   ├── popover.tsx
│   ├── scroll-area.tsx
│   └── tabs.tsx
│
├── layout/
│   ├── sidebar.tsx                 # 좌측 사이드바
│   ├── header.tsx                  # 상단 헤더
│   ├── mobile-nav.tsx              # 모바일 내비게이션
│   └── split-view.tsx              # Split View 컨테이너
│
├── cards/
│   ├── card-list.tsx               # 명함 리스트 래퍼
│   ├── card-grid-view.tsx          # 카드형 그리드
│   ├── card-list-view.tsx          # 리스트형
│   ├── card-table-view.tsx         # 테이블형
│   ├── card-item.tsx               # 개별 카드 아이템
│   ├── card-detail.tsx             # 상세 정보 패널
│   ├── card-form.tsx               # 생성/편집 폼 (RHF + Zod)
│   ├── card-search-bar.tsx         # 검색바 (Debounce 300ms)
│   ├── card-filter-bar.tsx         # 필터/정렬 바
│   ├── card-selection-bar.tsx      # 다중 선택 액션 바
│   ├── card-quick-actions.tsx      # 빠른 액션 (전화/이메일/...)
│   └── contact-detail-fields.tsx   # 다중값 전화/이메일 필드
│
├── scan/
│   ├── file-upload-zone.tsx        # 드래그앤드롭 업로드
│   ├── camera-capture.tsx          # WebRTC 카메라 캡처
│   ├── ocr-result-form.tsx         # OCR 결과 확인/편집 폼
│   └── scan-progress.tsx           # OCR 처리 진행 표시
│
├── folders/
│   ├── folder-tree.tsx             # 트리뷰
│   ├── folder-item.tsx             # 폴더 아이템
│   ├── folder-create-dialog.tsx    # 생성 다이얼로그
│   ├── folder-edit-dialog.tsx      # 편집 다이얼로그
│   └── folder-select-modal.tsx     # 폴더 선택 모달 (이동용)
│
├── tags/
│   ├── tag-list.tsx                # 태그 목록
│   ├── tag-badge.tsx               # 태그 뱃지
│   ├── tag-create-dialog.tsx       # 생성 다이얼로그
│   ├── tag-color-picker.tsx        # 컬러피커
│   └── tag-select-modal.tsx        # 태그 선택 모달
│
├── auth/
│   ├── login-form.tsx              # 로그인 폼
│   ├── register-form.tsx           # 회원가입 폼
│   ├── forgot-password-form.tsx    # 비밀번호 재설정 요청 폼
│   ├── reset-password-form.tsx     # 비밀번호 재설정 실행 폼
│   ├── social-login-buttons.tsx    # Google/Apple/Kakao 소셜 버튼
│   └── email-verify-status.tsx     # 이메일 인증 상태
│
├── settings/
│   ├── profile-form.tsx            # 프로필 편집
│   └── export-wizard.tsx           # 내보내기 위자드
│
└── common/
    ├── empty-state.tsx             # 빈 상태 표시
    ├── loading-spinner.tsx         # 로딩 스피너
    ├── error-boundary.tsx          # 에러 바운더리
    ├── infinite-scroll.tsx         # 무한 스크롤 래퍼
    ├── confirmation-dialog.tsx     # 확인/삭제 다이얼로그
    ├── color-picker.tsx            # 색상 선택기
    └── image-viewer.tsx            # 이미지 확대 뷰어
```

---

## 4. 페이지별 상세

### 4.1 로그인 (`(auth)/login/page.tsx`)

**UI 요소:**
- 이메일 입력 (validation: email format)
- 비밀번호 입력 (min 8, 규칙 표시)
- "로그인 상태 유지" 체크박스
- 로그인 버튼
- "비밀번호를 잊으셨나요?" 링크
- 소셜 로그인 구분선 + Google/Apple/Kakao 버튼
- "계정이 없으신가요? 회원가입" 링크

**동작:**
- React Hook Form + loginSchema (Zod)
- 로그인 실패 시 에러 메시지 표시
- 5회 실패 시 잠금 상태 메시지
- 성공 시 `/cards`로 리디렉트

### 4.2 회원가입 (`(auth)/register/page.tsx`)

**UI 요소:**
- 이름 입력
- 이메일 입력 (실시간 중복 체크 - debounce)
- 비밀번호 입력 (강도 표시: 약/보통/강)
- 비밀번호 확인 입력
- 이용약관 / 개인정보처리방침 동의
- 회원가입 버튼
- 소셜 로그인 구분선 + Google/Apple/Kakao 버튼

**동작:**
- registerSchema: 비밀번호 8자+, 영문+숫자+특수문자
- 비밀번호 강도 실시간 피드백
- 성공 시 이메일 인증 안내 페이지로 이동

### 4.3 비밀번호 재설정 (`(auth)/forgot-password/page.tsx`)

**UI:** 이메일 입력 → 재설정 링크 발송 → 안내 메시지

### 4.4 이메일 인증 (`(auth)/verify-email/page.tsx`)

**UI:** URL의 token 파라미터로 자동 인증 시도 → 성공/실패 표시

### 4.5 온보딩 (`(auth)/onboarding/page.tsx`)

**UI:** 2-3단계 슬라이드
1. "CardKeeper에 오신 것을 환영합니다" + 핵심 기능 소개
2. "첫 명함을 등록해보세요" + 스캔/수동 입력 선택
3. 시작하기

---

### 4.6 명함 목록 (`(main)/cards/page.tsx`)

**레이아웃:**
```
┌─────────────────────────────────────────────┐
│ 검색바 [🔍 이름, 회사, 전화번호 검색...]      │
├─────────────────────────────────────────────┤
│ 필터: [폴더▼] [태그▼] [즐겨찾기] [날짜▼]     │
│ 정렬: [최신순▼]   뷰: [카드|리스트|테이블]     │
├─────────────────────────────────────────────┤
│                                             │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐           │
│ │카드1 │ │카드2 │ │카드3 │ │카드4 │ (카드뷰)  │
│ └─────┘ └─────┘ └─────┘ └─────┘           │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐           │
│ │카드5 │ │카드6 │ │카드7 │ │카드8 │           │
│ └─────┘ └─────┘ └─────┘ └─────┘           │
│                                             │
│ [무한 스크롤 로딩...]                         │
├─────────────────────────────────────────────┤
│ 총 156건                     [+ 새 명함]     │
└─────────────────────────────────────────────┘
```

**주요 기능:**
- **뷰 토글:** 카드(Grid) / 리스트(List) / 테이블(Table) - Zustand uiStore에 저장
- **검색:** Debounce 300ms, 최근 검색어 10개 (localStorage)
- **필터:** 폴더(드롭다운), 태그(다중 선택), 즐겨찾기(토글), 날짜 범위(DatePicker)
- **정렬:** 이름순/최신순/수정순/회사명순 + 오름차순/내림차순
- **무한 스크롤:** TanStack Query useInfiniteQuery + Intersection Observer
- **다중 선택:** 체크박스 클릭 → 상단에 선택 액션 바 표시 (이동/태그/삭제)
- **빈 상태:** 명함 없을 때 안내 + "첫 명함 등록하기" CTA

### 4.7 카드형 그리드 뷰 (`card-grid-view.tsx`)

```
┌────────────────────────┐
│ ☐  [썸네일 이미지]  ★   │
│                        │
│ 김철수                  │
│ CTO                    │
│ 테크코리아 주식회사      │
│                        │
│ 📞 010-1234-5678       │
│ ✉️ chulsoo@tech...     │
│                        │
│ 🏷️ VIP  📂 IT 업계    │
└────────────────────────┘
```

### 4.8 테이블 뷰 (`card-table-view.tsx`)

| ☐ | 이름 | 회사 | 직함 | 전화번호 | 이메일 | 폴더 | 태그 | ★ | 등록일 |
|---|------|------|------|---------|--------|------|------|---|--------|
| ☐ | 김철수 | 테크코리아 | CTO | 010-1234-5678 | chulsoo@... | IT 업계 | VIP | ★ | 01.15 |

- 열 정렬 가능 (헤더 클릭)
- 행 클릭 → Split View 상세 표시

---

### 4.9 스캔 화면 (`(main)/scan/page.tsx`)

**UI 요소:**
1. **파일 업로드 영역** (기본)
   - 드래그앤드롭 존 (react-dropzone)
   - 클릭하여 파일 선택
   - 지원 형식: JPEG, PNG, HEIC
   - 최대 10MB
   - 미리보기 표시

2. **WebRTC 카메라** (선택)
   - 카메라 활성화 버튼
   - 뷰파인더 + 명함 프레임 가이드 오버레이
   - 촬영 버튼
   - 전/후면 카메라 전환

3. **업로드 진행**
   - 프로그레스 바
   - "이미지 업로드 중..." → "OCR 처리 중..." → "완료!"

**동작:**
- 이미지 선택/촬영 → POST /api/v1/scan/upload
- 성공 시 → `/scan/confirm?scanId=xxx`로 이동

### 4.10 OCR 결과 확인/편집 (`(main)/scan/confirm/page.tsx`)

**레이아웃:**
```
┌──────────────────┬──────────────────────────┐
│                  │                          │
│ [스캔 이미지]     │  확인된 정보              │
│  원본 표시       │                          │
│  확대/축소       │  이름: [김철수         ]   │
│                  │  회사: [테크코리아     ]   │
│                  │  직함: [CTO           ]   │
│                  │                          │
│                  │  전화: [010-1234-5678 ] + │
│                  │  이메일: [chulsoo@... ] + │
│                  │                          │
│                  │  주소: [서울시 강남구..]   │
│                  │  웹사이트: [www.tech..]   │
│                  │  메모: [             ]    │
│                  │                          │
│                  │  폴더: [미분류 ▼]         │
│                  │  태그: [+ 태그 추가]      │
│                  │                          │
│                  │  [취소]   [저장]           │
└──────────────────┴──────────────────────────┘
```

**주요 기능:**
- 좌측: OCR 원본 이미지 (확대 가능)
- 우측: 파싱된 필드 (수정 가능)
- 신뢰도 낮은 필드 하이라이트 표시
- 전화/이메일: "+" 버튼으로 다중값 추가
- 폴더/태그 선택
- 저장 → POST /api/v1/scan/confirm

---

### 4.11 수동 입력 폼 (`(main)/cards/new/page.tsx`)

**데이터 패칭:**
- `useFolders()` 훅으로 폴더 목록 조회 (TanStack Query, queryKey: `["folders"]`)
- `useTags()` 훅으로 태그 목록 조회 (TanStack Query, queryKey: `["tags"]`)
- 폴더/태그 생성 시 각각의 mutation이 해당 queryKey를 invalidate → 목록 자동 갱신
- `?? []` 방어 처리로 데이터 로딩 중/null 상태에서도 안전하게 렌더링

**React Hook Form + Zod:**
```typescript
// 폼 스키마 (shared-utils에서 import)
const cardFormSchema = z.object({
  name: z.string().min(1, "이름을 입력해주세요").max(100),
  company: z.string().max(100).optional(),
  jobTitle: z.string().max(100).optional(),
  contactDetails: z.array(z.object({
    type: z.enum(["PHONE", "EMAIL", "FAX", "MOBILE", "OTHER"]),
    label: z.string().max(50).optional(),
    value: z.string().min(1),
    isPrimary: z.boolean(),
  })).min(0),
  address: z.string().max(500).optional(),
  website: z.string().url().or(z.literal("")).optional(),
  memo: z.string().max(2000).optional(),
  folderId: z.string().optional(),
  tagIds: z.array(z.string()).max(10).optional(),
});
```

**다중값 전화/이메일 필드:**
- 기본 1개 필드 표시
- "+" 버튼으로 추가 (useFieldArray)
- "-" 버튼으로 제거
- 라벨 선택: "업무용", "개인", "기타"
- 기본(Primary) 라디오 버튼

**자동저장 (Draft):**
- Debounce 3초마다 localStorage에 Draft 저장 (Zustand draftStore)
- 페이지 이탈 시 경고 (beforeunload)
- 재방문 시 Draft 복원 제안

**자동완성:**
- 회사명: 기존 명함의 회사명 목록에서 제안
- 직함: 일반적인 직함 목록에서 제안

---

### 4.12 명함 상세/편집 (`(main)/cards/[id]/page.tsx`)

**Split View 패널 (Desktop):**
```
┌──────────────────┬──────────────────────────┐
│                  │  김철수  ★               │
│                  │  CTO · 테크코리아         │
│                  │                          │
│ [명함 리스트]     │  [전화] [이메일] [지도] [웹] │
│                  │                          │
│                  │  📞 업무용 010-1234-5678  │
│                  │  ✉️ chulsoo@techkorea.com │
│                  │  🏠 서울시 강남구...       │
│                  │  🌐 www.techkorea.com     │
│                  │                          │
│                  │  📂 IT 업계              │
│                  │  🏷️ VIP · 파트너         │
│                  │                          │
│                  │  메모:                    │
│                  │  2026.01 CES 컨퍼런스에서  │
│                  │                          │
│                  │  [원본 이미지 보기]        │
│                  │                          │
│                  │  등록: 2026.01.15         │
│                  │  수정: 2026.01.20         │
│                  │                          │
│                  │  [편집] [삭제] [공유]      │
└──────────────────┴──────────────────────────┘
```

**인라인 편집:**
- 각 필드 옆 편집 아이콘 클릭 → 인라인 입력 활성화
- Enter/Blur로 저장 (PUT /api/v1/cards/:id)
- Escape로 취소

**빠른 액션:**
- 전화 걸기: `tel:` 링크
- 이메일 보내기: `mailto:` 링크
- 주소 지도 열기: Google Maps URL
- 웹사이트 열기: 새 탭

---

### 4.13 폴더 관리 (`(main)/folders/page.tsx`)

**트리뷰:**
```
📁 IT 업계 (23)              [편집] [삭제]
  └── 📁 스타트업 (8)         [편집] [삭제]
📁 금융 (15)                  [편집] [삭제]
📁 미분류 (45)                [기본]
                              [+ 새 폴더]
```

**드래그앤드롭:**
- 폴더 간 순서 변경 (react-dnd 또는 @dnd-kit/core)
- 명함을 폴더로 드래그하여 이동

**폴더 생성/편집 다이얼로그:**
- 이름 입력
- 색상 선택 (프리셋 팔레트)
- 부모 폴더 선택 (최상위만 선택 가능 - 1단계 제한)

---

### 4.14 태그 관리 (`(main)/tags/page.tsx`)

**UI:**
```
🏷️ VIP (12)     [#EF4444] [편집] [삭제]
🏷️ 파트너 (8)    [#3B82F6] [편집] [삭제]
🏷️ 고객사 (15)   [#10B981] [편집] [삭제]

[+ 새 태그]
```

**태그 생성/편집:**
- 이름 입력 (최대 30자)
- 컬러피커 (프리셋 12색 + 커스텀 HEX)

---

### 4.15 설정 (`(main)/settings/`)

**설정 메인:**
- 프로필 (이름, 이메일, 아바타)
- 내보내기
- 도움말 / FAQ
- 로그아웃
- 계정 삭제

**프로필 편집:**
- 이름 변경
- 아바타 업로드
- 비밀번호 변경 (현재 비밀번호 확인)

**내보내기 위자드:**
1. 범위 선택: 전체 / 폴더별 / 태그별 / 직접 선택
2. 형식 선택: vCard 3.0 / vCard 4.0 / CSV
3. 미리보기 (건수 표시)
4. 내보내기 실행 → 파일 다운로드

---

## 5. 반응형 디자인

### 5.1 브레이크포인트

| 이름 | 크기 | 레이아웃 |
|------|------|---------|
| `sm` | 640px+ | 모바일 기본 (단일 컬럼) |
| `md` | 768px+ | 태블릿 (사이드바 축소) |
| `lg` | 1024px+ | 데스크톱 (사이드바 + Split View) |
| `xl` | 1280px+ | 와이드 (3컬럼 가능) |
| `2xl` | 1536px+ | 초와이드 |

### 5.2 브레이크포인트별 변화

| 요소 | Mobile (<768px) | Tablet (768-1023px) | Desktop (≥1024px) |
|------|-----------------|--------------------|--------------------|
| 사이드바 | 햄버거 Sheet | 아이콘만 | 전체 표시 |
| 명함 뷰 | 카드 1열 | 카드 2열 / 리스트 | 카드 3-4열 / 테이블 |
| 상세 화면 | 전체 페이지 | 전체 페이지 | Split View 패널 |
| 검색바 | 아이콘 → 확장 | 중간 너비 | 전체 너비 |
| 다중 선택 | 롱프레스 | 체크박스 | 체크박스 |
| OCR 확인 | 세로 스택 | 세로 스택 | 좌우 Split |

---

## 6. 핵심 Hooks

```
apps/web/src/hooks/
├── use-cards.ts              # TanStack Query: 명함 CRUD
├── use-card-detail.ts        # 명함 상세 조회
├── use-infinite-cards.ts     # 무한 스크롤 명함 목록
├── use-folders.ts            # 폴더 CRUD
├── use-tags.ts               # 태그 CRUD
├── use-scan.ts               # OCR 스캔 mutation
├── use-export.ts             # 내보내기 mutation
├── use-auth.ts               # 인증 상태/액션
├── use-search.ts             # 검색 (debounce, 히스토리)
├── use-media-query.ts        # 반응형 브레이크포인트
├── use-intersection.ts       # Intersection Observer
└── use-debounce.ts           # Debounce 유틸
```

---

**다음 문서:** [04-frontend-mobile.md](./04-frontend-mobile.md)
